#include <stdio.h>
#include <cs50.h>
#include <string.h>
int main ()
{
    string r = get_string("Введіть рядок: ");
    for(int i = strlen(r) - 1; i >= 0; i--)
    {
        printf("%c", r[i]);
    }
    return 0;
}
